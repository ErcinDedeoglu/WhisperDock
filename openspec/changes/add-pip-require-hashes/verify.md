# Verify

## Gates

- unittest 6/6; holdout missing-file 400 JSON
- `whisperdock-local:pinned` metadata match freeze
- pip install used `--require-hashes`; MarkupSafe aarch64 wheel accepted
- Chrome: N/A — no UI

## Remote

Pending origin push and GHA amd64 hash check.
