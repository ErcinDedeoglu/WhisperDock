## 1. README command

- [x] 1.1 In `README.md` Getting Started “Building from Source”, change `docker build -t whisperdock .` to `docker build -t whisperdock src` and verify the file contains `docker build -t whisperdock src` and does not contain `docker build -t whisperdock .`
