## 1. README

- [x] 1.1 Replace the WAV-16kHz-only sentence in `README.md` API Usage with a sentence that the service converts ffmpeg-readable audio to 16 kHz mono WAV (16 MB max), then verify the old phrase is gone and `cd src && python3 -m unittest test_app.py` exits 0
