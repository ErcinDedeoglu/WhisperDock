## 1. README

- [x] 1.1 Remove the leftover “Adjust the example response…” paragraph (and the extra `---` above it) from `README.md` and verify that phrase is gone while the JSON examples remain, and `cd src && python3 -m unittest test_app.py -v` exits 0
