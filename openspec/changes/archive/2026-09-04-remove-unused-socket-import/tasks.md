## 1. Remove import

- [x] 1.1 Delete `import socket` from `src/app.py` and verify the file has no `import socket` and `cd src && python3 -m unittest test_app.py -v` exits 0 including holdout missing-file JSON 400
