# Verify

## Gates

- `cd src && python3 -m unittest test_app.py` — exit 0, 6/6
- Holdout missing-file: 400 JSON `{"error":"No file part"}`
- `whisperdock-local:pinned` metadata: `3.1.3 26.2.0 3.1.8 3.1.6 3.0.3 1.9.0 8.5.0 2.2.0`
- Dockerfile: `pip install --no-cache-dir --no-deps -r /app/requirements.txt`
- Chrome: N/A — no UI

## Remote

Pending origin push and GHA.
